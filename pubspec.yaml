name: architecture_flutter
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6

  #Core
  equatable: ^2.0.5

  #Network Dependencies
  vexana: ^4.0.4

  #Utility
  kartal: ^4.0.4

  widgets:
    path: module/widgets
  

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.11
  very_good_analysis: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - asset/translations/
